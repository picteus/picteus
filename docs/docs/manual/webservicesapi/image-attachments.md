# Image attachments

Image attachments associate arbitrary binary files with images.

| Method | Endpoint | Purpose |
|:---|:---|:---|
| `POST` | `/imageAttachment/create` | Declares and stores an image binary attachment. |
| `GET` | `/imageAttachment/{uri}/download` | Downloads an attachment. |

Use the OpenAPI specification for attachment metadata, URI encoding, permissions, and response details.

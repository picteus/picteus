{
  "$schema": "https://picteus.github.io/picteus/jsonschema/manifest-v2.schema.json",
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "categories": [],
  "runtimes": [
    {
      "environment": "python"
    }
  ],
  "instructions": [
    {
      "events": [
        "process.started", "image.created", "image.updated", "image.deleted", "image.runCommand"
      ],
      "throttlingPolicies": [
        {
          "events": ["image.created", "image.updated", "image.runCommand"],
          "maximumCount": 4
        }
      ],
      "execution": {
        "executable": "${venvPython}",
        "arguments": ["./main.py"]
      },
      "commands": [
        {
          "id": "commandId",
          "on": {
            "entity": "Images"
          },
          "specifications": [
            {
              "locale": "en",
              "label": "Command",
              "name": "Command example",
              "description": "Logs in the application the identifier of the run command along with the identifiers of the images it applies to."
            }
          ],
          "ui": {
            "iconUri": "/assets/commandId.svg"
          }
        }
      ]
    }
  ],
  "settings": {
    "type": "object",
    "properties": {
      "parameter": {
        "type": "string",
        "title": "Parameter",
        "description": "A parameter which enables to tune the extension."
      }
    },
    "required": ["parameter"]
  }
}
